# PHYSICS_REVIEW.md — 물리 검증 노트

CLAUDE.md의 동반 문서다. CLAUDE.md가 "무엇을 어떤 순서로"라면, 이 문서는
"왜 그것이 비약인지, 올바른 물리는 무엇인지, 어떤 수치로 검증(백테스트)하는지"다.
**CLAUDE.md 7절 로드맵의 각 항목을 구현하기 전, 이 문서의 해당 절을 반드시 읽는다.**

- 작성: 2026-07-04. 코드 전수 재확인 + 수치는 Python으로 직접 계산해 검증함.
- 모든 기준값·계수는 소유자 승인 전까지 잠정이며 `REVIEW` 표기.
- 코드 인용의 줄 번호는 작성 시점 기준이므로 참고용. 스니펫으로 검색해 찾을 것.

## 분류 체계

| 태그 | 의미 | 처리 |
|---|---|---|
| **[E]** | 물리 오류 | 반드시 교정 |
| **[L]** | 논리/구현 결함 (물리 이전의 비약) | 반드시 교정 |
| **[S]** | 의도적 단순화지만 오해 소지 | 개선하거나, 불가하면 Known Limitations에 명시 |
| **[A]** | 허용되는 단순화 | 유지. README에 문서화만 |

---

# 1. Fab Simulator

## 1.1 [E] 열산화가 '증착'으로 구현됨 (G1)

**현재 코드** — `case 'furnace': deposit('SiO2', time * 0.5)`
표면 재질과 무관하게 성장하고, Si를 소모하지 않으며, 속도가 선형 고정이다.
세 가지가 모두 물리와 다르다.

**올바른 물리**
1. 열산화는 **노출된 Si·Poly-Si·도핑 Si(Si-n/Si-p) 표면에서만** 일어난다.
   Al·PR·SiO2 위 성장은 0. (도핑 Si 포함은 2026-07-06 소유자 승인.)
   (산화종이 기존 산화막을 확산 통과해 Si 계면에서 반응 — 그래서 기존 산화막이
   있으면 성장이 느려진다.)
2. **Si 소모**: 성장한 산화막 두께의 0.44배만큼 Si가 소모된다.
   유도: 몰밀도비 N(SiO2)/N(Si) = 2.2×10²² / 5.0×10²² cm⁻³ = 0.44.
   따라서 표면은 원래 위치보다 0.56·x_ox 만큼만 올라간다.
3. **Deal-Grove 선형–포물선 성장**: x² + A·x = B·(t + τ).
   얇을 땐 반응율속(선형, 속도 B/A), 두꺼워지면 확산율속(포물선, 계수 B).
   기존 산화막 x₀는 등가 시간 τ_eq = (x₀² + A·x₀)/B 로 반영.

**대표 파라미터 (1000°C, 교과서 표준값, `REVIEW`)**

| 모드 | A (µm) | B (µm²/h) | τ (h) |
|---|---|---|---|
| Dry O₂ | 0.165 | 0.0117 | 0.37 |
| Wet H₂O | 0.226 | 0.287 | 0 |

**계산 검증 결과** (이 값이 곧 테스트 기대값)
- dry 1000°C 60분, 맨 Si → x_ox ≈ **69 nm** (문헌 차트 60–70nm 범위와 일치)
- wet 1000°C 60분 → x_ox ≈ **435 nm** (문헌 400–450nm 범위와 일치)
- wet 30분: 맨 Si에서 282nm 성장 vs 기존 산화막 50nm 위에서 249nm 추가 성장 → 둔화 재현
- 산화막 100nm 성장 시: Si −44nm, 표면 상승 +56nm

**권장 구현**
```
oxidize(columnStack, { mode, timeMin }) — 순수 함수
1) 최상층이 Si/Poly-Si인지, 또는 그 위가 SiO2뿐인지 확인. 아니면 성장 0 + 사유 반환
2) 기존 SiO2 두께 x0 → τ_eq 계산 → Δx = x(t+τ_eq) − x0
3) Si(또는 Poly) 층 두께 −0.44·Δx, SiO2 층 +Δx (없으면 생성)
4) 반환: { oxideGrown, siConsumed, reason }
```
장비 모달에 Wet 옵션 추가(현재 Dry뿐). 온도는 1000°C 고정으로 시작 (온도 입력은 선택 확장).

**백테스트**
- dry 60min bare Si → oxideGrown ∈ 69nm ±5%, siConsumed = 0.44·oxideGrown
- wet 60min → 435nm ±5%
- 기존 50nm 위 wet 30min 성장 < bare 30min 성장
- 최상층 Al → oxideGrown = 0
- 최상층 PR → oxideGrown = 0 (실제론 PR이 타지만, 그건 1.5의 온도 규칙이 잡는다)

## 1.2 [E] Implant 마스킹 버그 (G2) + [S] 도핑 깊이 부재

**현재 코드** — `waferStack.find(l => l.L>0 && l.mat==='Si').mat = newSi`
`mat`이 레이어 전역 속성이라, **한 컬럼만 노출돼도 마스크로 가린 컬럼까지 도핑**된다.
마스킹이라는 리소그래피의 존재 이유가 무력화되는 버그. 근본 원인은 데이터 모델.
추가로, 레이어 두께와 무관하게 전체가 변환된다 — 이온주입은 표면 근방(투영비정
R_p 부근)에 분포하므로 500nm Si가 통째로 도핑되는 것은 비약이다.

**구현 기준** (Phase 0의 새 데이터 모델 위에서 — 2026-07-06 소유자 승인)
- 노출된 컬럼만 도핑. 변환 깊이 d_imp = min(층 두께, D_imp). **D_imp = 100nm 승인**.
- 층이 D_imp보다 두꺼우면 분할: 상부 d_imp만 도핑 변종으로, 하부는 원래 재질 유지.
- **도핑 대상 = 단결정 Si 계열 + Poly-Si 계열** (poly 게이트 도핑은 표준 공정 —
  소유자 지시로 포함). 도핑된 poly는 Poly-Si-n/Poly-Si-p로 표기(결정성 유지).
- **역도핑(카운터 도핑) 허용** — n↔p 재주입 시 상부 d_imp만 재변환.
- 도즈/에너지 파라미터는 도입하지 않는다(A급 단순화로 유지).

**백테스트**
- C만 노출(L/R에 PR) 상태에서 implant → L/R의 재질 불변 (G2 회귀 테스트)
- 300nm Si, D_imp=100 → 상부 100nm Si-n + 하부 200nm Si로 분할
- Poly-Si 200nm에 n implant → 상부 100nm Poly-Si-n + 하부 100nm Poly-Si

## 1.3 [S] 증착 conformality 미구현 (G3)

**현재** — 모든 증착 장비가 전 컬럼 균일 증착. LPCVD의 "Excellent Step Coverage"는
문구뿐이라, 사용자가 문구와 거동의 불일치를 학습하게 되는 것이 문제.

**물리** — step coverage = (측벽/바닥 두께) / (상면 두께). 표면 반응율속(ALD, LPCVD)일수록
컨포멀, 도달 플럭스 지배(PVD 스퍼터)일수록 비컨포멀. 종횡비가 클수록 악화(향후 확장).

**계수** — 2026-07-06 소유자 승인: ALD 1.00 / LPCVD 0.90 / PECVD 0.60 / PVD(Endura) 0.30
(단차 유무 이분법 포함. AR 의존은 Phase 4 C6에서 확장.)

**권장 구현** — 컬럼 간 높이차(단차)가 있을 때, 낮은 컬럼(트렌치 바닥)에는
계수 × 명목두께만 증착. 단차가 없으면 전 컬럼 균일.

**백테스트** — 100nm 단차 구조에 PVD 100nm → 바닥 30nm / 같은 구조 ALD → 100nm.

## 1.4 [S] 식각: 무한 선택비 + 완전 이방성 (G4)

**현재** — 다른 재질을 만나면 무조건 정지(무한 선택비), over-etch 손실 없음,
wet 언더컷 없음, 테이퍼 개념 없음.

**물리 기준** — 2026-07-06 소유자 승인 (교육용 방향·자릿수 기준.
미정의 재질쌍 기본값 10 포함. wet 언더컷 1:1 근사도 동일 일자 승인.)
- 선택비 S = ER(타깃)/ER(하부막). over-etch 시간 t_ov 동안 하부막 손실 = t_ov·ER_타깃/S.

| 공정 | 선택비 (잠정) |
|---|---|
| RIE oxide etch (CHF₃계) | SiO₂:Si ≈ 8:1, SiO₂:PR ≈ 4:1 |
| RIE CF₄/O₂ | Si:SiO₂ ≈ 2:1 (O₂ 증가 시 F 라디칼 증가 → Si 쪽 유리) |
| DRIE (Bosch) | Si:SiO₂ ≥ 50:1 |
| BOE (wet) | SiO₂:Si ≥ 100:1 |
| PAN (wet) | Al만 식각 |

- **wet 등방성**: 언더컷 ≈ 식각 깊이(측면:수직 ≈ 1:1).
- **dry 테이퍼각**: L/C/R 3컬럼 기하로는 측벽각을 표현할 수 없다.
  **무리한 구현 금지** — Known Limitations에 명시하고, 필요하면 단면 뷰에
  정보 뱃지(예: "실제 RIE 측벽은 수직에 가깝고 wet은 등방")로만 전달.

**권장 구현**
- dry: 재질쌍 선택비 테이블 참조, 타깃 관통 후 잔여 시간만큼 하부막을 S로 나눠 소모.
- wet: 노출 개구부에 인접한 컬럼의 해당 층에 `undercut` 스칼라(좌/우 nm)를 기록하고
  단면 뷰에서 notch로 렌더링. 컬럼 두께 배열 자체는 건드리지 않는다(모델 한계 존중).

**백테스트**
- oxide 200nm를 RIE로 20% over-etch → Si 손실 ≈ 0.2·200/8 = 5nm
- BOE로 SiO₂ 100nm 제거 → 인접 컬럼 undercut 기록 ≈ 100nm
- PAN에 SiO₂ 노출 → 손실 0

## 1.5 [L] 공정 순서 호환성 검사 부재 (신규 L1) — 교육 가치 최상

**현재** — Al을 증착한 뒤 1000°C Furnace에 넣어도 아무 일도 일어나지 않는다.
Al 융점은 660°C다. PR이 있는 웨이퍼를 300°C PECVD에 넣어도 무경고다(PR 한계 ~130–150°C).
개별 공정이 다 맞아도 **순서가 틀리면 소자가 죽는다**는, 공정 통합의 제1 감각이
모델에 없다.

**권장 구현** — 재료 최대 허용온도 × 장비 공정온도 규칙 검사. 실행 전 검사해서
위반 시 경고 배너 + 공정 로그에 기록.
**2026-07-06 소유자 확정: "경고 후 진행"이 기본** — 교육적으로 실수의 결과를 보게 한다.
완전 차단은 Phase 4 미션 모드에서 실패 판정으로 활용.

온도 테이블 — 2026-07-06 소유자 승인:

| 재료 한계 | 온도 | | 장비 공정온도 | 온도 |
|---|---|---|---|---|
| PR | 130°C | | Furnace | 1000°C |
| Al | 450°C | | LPCVD | 600°C |
| 기타 | 제한 없음 | | PECVD | 300°C |
| | | | ALD | 250°C |
| | | | 그 외 | ~실온 |

**백테스트** — Al 존재 + Furnace → warning 발생 / PR 존재 + PECVD → warning /
PR 없음 + PECVD → warning 없음.

## 1.6 [L/S] Metrology 표현 보강

- 단면 뷰가 auto-scale이라 **절대 스케일 정보가 없다** → nm 스케일 바 추가.
- 공정 이력 로그 스키마 제안:
  `{ step, equip, params, perColumn:{L,C,R 총두께}, stepHeight, target?, delta? }`
  레시피 모달에 target(선택) 입력 → 실행 후 편차 표시.
- (선택) 최상층이 투명막(SiO₂)일 때 두께→간섭색 근사 표시(정성 대응표).
  측정값을 색으로 교차확인하는 감각을 주는 장치.
- **구현 확정 (2026-07-06 소유자 승인)**: 측정 기준은 '높이 변화'로 통일,
  편차 색상 판정 ±5%, 이력 테이블 UI 승인. 단, 산화 공정 행에서는
  산화막 성장 두께와 표면 상승(0.56×성장)을 **둘 다 표시**해 Si 소모가
  오해 없이 드러나게 한다 (소유자 지시).

## 1.7 [A] 허용되는 단순화 (Fab) — README Known Limitations로

증착·식각의 선형 속도 / 도즈·에너지·확산 프로파일 없음 / 열이력(도판트 재분포) 없음 /
CMP 없음 / 3컬럼 기하(연속 프로파일·측벽각 표현 불가) / 결정성·응력·계면 상태 없음 /
dry 산화의 초기 급속 성장 구간을 τ=0.37h(등가 산화막 ~23nm)로 근사 —
기존 산화막이 23nm 미만이면 이력이 23nm 등가로 취급됨
(τ_eff = max(τ, τ_eq), 2026-07-06 소유자 수용).

---

# 2. DRAM Simulator

## 2.1 [E] Write에 상한이 없음 (G5)

**현재** — `maxChargeForBL = (bl/3)*100`: WL이 Vth만 넘으면 셀이 BL 레벨까지
무조건 충전된다. WL 전압의 크기는 통과 상한에 아무 역할이 없다.

**물리** — NMOS 패스 트랜지스터로 '1'을 쓸 때 저장노드는 소스처럼 동작하므로
V_storage가 V_WL − Vth에 도달하면 V_GS = Vth가 되어 차단된다:
**V_storage(max) = min(V_BL, V_WL − Vth)**.

**계산 검증 결과** (Vth = 1.0V)

| WL | BL | V_storage 상한 |
|---|---|---|
| 1.5V | 3V | 0.5V |
| 2.0V | 3V | 1.0V |
| 3.0V | 3V | **2.0V** ← VDD인데도 full '1' 불가 |
| 4.2V | 3V | 3.0V ← 워드라인 승압(VPP ≥ VDD+Vth+margin)의 존재 이유 |

**권장 구현** — 충전 목표 전압 = min(V_BL, max(0, V_WL − Vth)).
charge 목표(%) = 목표전압/3 × 100. 방전 방향(BL이 셀보다 낮을 때)은 기존 로직 유지.
WL 슬라이더 범위를 0–4.5V로 확장하고 3V 위 구간에 "VPP 영역" 표시 + 한 줄 툴팁.

**백테스트** — WL=3, BL=3 장시간 → charge → 66.7% 수렴 / WL=1.5, BL=3 → 16.7% /
WL=4.2, BL=3 → 100%.

## 2.2 [E] Read가 단순 방전 (G6)

**현재** — BL을 낮추고 WL을 열면 셀이 그냥 방전된다. 프리차지도, 감지도,
재기록도 없어 "DRAM 읽기는 파괴적이며 반드시 복원한다"는 정수가 표현되지 않는다.

**물리** — BL을 VDD/2로 프리차지 → WL 개방 → 전하 재분배:
**ΔV_BL = (V_cell − VDD/2) · Cs/(Cs + C_BL)** → sense amp가 ΔV 부호를 감지 →
전 레일로 증폭하며 셀에 원 데이터를 **restore**.

**계산 검증 결과** (VDD=3V, 프리차지 1.5V, Cs=25fF, C_BL=100fF)

| 셀 상태 | ΔV_BL |
|---|---|
| '1' (3.0V) | **+300 mV** |
| '0' (0V) | **−300 mV** |
| 절반 누설 (1.5V) | **0 mV** — 감지 불가 경계 |

Cs/C_BL이 작아질수록 ΔV가 줄어든다 = 셀 커패시턴스 확보가 왜 사활인지의 정량적 이유.

**권장 구현** — READ 버튼 + 상태기계: precharge → share → sense → restore.
Cs:C_BL 비율 슬라이더(1:2 ~ 1:20). 감지 한계 |ΔV| ≥ ε (기본 50mV `REVIEW`),
미만이면 READ FAIL 표시. 각 단계에서 BL 전위와 ΔV를 화면에 수치로 표시.

**백테스트** — 위 표의 세 케이스 ΔV 수치(±1mV) / read '1' 완료 후 charge=100 복원 /
Cs:C_BL을 1:20으로 낮추면 같은 셀 전압에서 ΔV 축소 확인.

## 2.3 [L] Refresh가 '마법 재충전' (신규 L2)

**현재 코드 확인** — `refresh-btn` 클릭 시 `state.charge = 100;` **무조건**.
전하가 얼마나 남았든, 심지어 '0'이어야 할 셀도 100%가 된다. 실제 refresh는
read(감지) + restore이므로, **감지 임계 아래로 떨어진 뒤의 refresh는 데이터를
'0'으로 잘못 복원한다 — 즉 데이터 손실**이다. 현재 모델은 "refresh는 늦어도
상관없다"는 잘못된 직관을 심어준다.

**권장 구현** — REFRESH를 2.2의 read–restore 경로로 재구현.
charge ≥ 판정 임계(50% `REVIEW`)면 100으로, 미만이면 0으로 복원 + "DATA LOST" 이벤트 표시.

**백테스트** — charge 80 → refresh → 100 / charge 40 → refresh → 0 + 손실 이벤트 발생.

## 2.4 [S] 누설이 선형 상수 (신규 L3)

**현재 코드 확인** — `if (state.charge > 0) state.charge -= 0.02;` (프레임당 상수).

**물리** — 접합·GIDL 등 누설은 대략 저장 전압에 비례하고 온도에 지수적으로 민감하다.
1차 근사: **dQ/dt = −Q/τ(T)** (지수 감쇠), τ(T) = τ₂₅ · 2^(−(T−25)/10)
(온도 10°C 상승마다 리텐션 절반 — 업계 rule of thumb, `REVIEW`).
참고: subthreshold 스윙의 이론 하한은 ln(10)·kT/q = 59.5 mV/dec @300K (실제 70–90).

**권장 구현** — 지수 감쇠 + 온도 슬라이더(25–85°C), 현재 조건의 반감기
t_half = τ·ln2 를 화면에 표시.

**백테스트** — 동일 온도에서 100→50 소요시간 = 50→25 소요시간(반감기 일정) /
T +10°C → t_half 절반(±5%).

## 2.5 [L] 물리 갱신이 프레임 기반 (신규 L4) — Phase 0에서 처리

**현재 코드 확인** — `requestAnimationFrame(animate)` 루프에서 `charge ± 0.8`,
`charge -= 0.02` 등 **프레임당 상수**로 갱신. 60Hz와 144Hz 모니터에서 물리 속도가
2.4배 차이 난다. 물리가 하드웨어에 종속된 상태.

**권장 구현** — dt = (now − last)/1000 기반으로 전환하고 모든 rate를 초당 단위로
재정의: `stepCharge(state, dt)`. 이 전환은 **물리 함수 분리(Phase 0)와 동시에** 한다.

**백테스트** — stepCharge를 dt=0.5로 2회 호출한 결과 == dt=1.0으로 1회 호출한 결과
(누적 오차 허용범위 내).

## 2.6 [A] 허용되는 단순화 (DRAM) — README Known Limitations로

WL–장벽의 선형 근사(표면 퍼텐셜 유도 아님) / DIBL·바디이펙트·단채널 효과 없음 /
전류식·캐리어 통계 없음 / sense amp 회로 세부 없음 / 밴드 곡선은 smoothstep 시각화.

**단, 보존해야 할 불변식** (테스트로 고정할 것):
- 커패시터 충전 증가 → Source 밴드는 반드시 아래로 (전위↑ = 전자에너지↓)
- WL = Vth 지점에서 장벽 높이 연속

---

# 3. 백테스트 하네스 설계

**2단계 원칙**
1. **Characterization (Phase 0)** — 리팩토링 전에 현재 동작을 그대로 고정하는
   테스트를 먼저 작성한다(G1–G6의 잘못된 동작 포함). 목적: 구조 분리가 동작을
   바꾸지 않았음의 증명.
2. **Physics assertion (Phase 1–2)** — 각 교정 PR에서 해당 characterization
   테스트를 본 문서의 수치 기대값으로 교체한다. 교체 자체가 커밋 diff에 남아
   "무엇이 왜 바뀌었는지"의 기록이 된다.

**규칙** — 물리 함수는 순수 함수 / 테스트는 수치 + 허용오차 명시 /
E·L 항목당 최소 1개 테스트 / 버그 수정에는 회귀 테스트 필수.

**예시** (node:test, 외부 의존성 0):
```js
// tests/fab_oxidation.test.js
const test = require('node:test');
const assert = require('node:assert');
const { oxidize } = require('../js/fab_physics.js');

test('dry 1000C 60min on bare Si: ~69nm grown, 0.44x Si consumed', () => {
  const r = oxidize([{ mat: 'Si', L: 500, C: 500, R: 500 }], { mode: 'dry', timeMin: 60 });
  assert.ok(Math.abs(r.oxideGrown - 69) / 69 < 0.05);
  assert.ok(Math.abs(r.siConsumed - 0.44 * r.oxideGrown) < 0.5);
});

test('no growth on Al', () => {
  const r = oxidize([{ mat: 'Al', L: 300, C: 300, R: 300 }], { mode: 'dry', timeMin: 60 });
  assert.strictEqual(r.oxideGrown, 0);
});
```

**수치 테스트 총괄표** (기대값 출처는 각 절)

| # | 항목 | 입력 | 기대값 | 오차 |
|---|---|---|---|---|
| T1 | 산화 dry | 1000°C 60min, bare Si | x_ox 69nm, Si −0.44x | ±5% |
| T2 | 산화 wet | 1000°C 60min | 435nm | ±5% |
| T3 | 산화 둔화 | 기존 50nm 위 wet 30min | < bare 30min(282nm) | — |
| T4 | 산화 차단 | 최상층 Al/PR | 성장 0 | 정확 |
| T5 | Implant 마스킹 | C만 노출 | L/R 불변 | 정확 |
| T6 | Implant 깊이 | Si 300nm, D=100 | 100/200 분할 | 정확 |
| T7 | Conformality | 단차 100nm, PVD 100nm | 바닥 30nm | ±10% |
| T8 | Over-etch | oxide 200nm RIE +20% | Si 손실 ~5nm | ±20% |
| T9 | Undercut | BOE 100nm | 기록 ~100nm | ±10% |
| T10 | 온도 규칙 | Al+Furnace / PR+PECVD | warning | 정확 |
| T11 | Write cap | WL3/BL3, WL1.5/BL3, WL4.2/BL3 | 66.7/16.7/100% | ±1%p |
| T12 | ΔV | '1'/'0'/1.5V | +300/−300/0 mV | ±1mV |
| T13 | Restore | read '1' 완료 | charge 100 | 정확 |
| T14 | Refresh 손실 | charge 40 → refresh | 0 + 이벤트 | 정확 |
| T15 | 반감기 | 동일 T | t(100→50)=t(50→25) | ±5% |
| T16 | 온도-리텐션 | T+10°C | t_half 절반 | ±5% |
| T17 | dt 불변 | 0.5×2 vs 1.0×1 | 결과 동일 | 수치오차 |
| T18 | 불변식 | 충전↑ | Source 밴드 y 증가(아래로) | 정확 |

---

# 4. 우선순위 매트릭스

| 순서 | 항목 | 태그 | 난이도 | 비고 |
|---|---|---|---|---|
| 0 | 물리 분리 + dt 기반(2.5) + characterization | L | 중 | 모든 것의 전제 |
| 1 | 열산화 교정 (1.1) | E | 중 | 대표 교정 사례 |
| 2 | 식각 선택비·언더컷 (1.4) | S | 중 | |
| 3 | Conformality (1.3) | S | 하 | |
| 4 | Implant 마스킹+깊이 (1.2) | E | 중 | 새 데이터 모델 필요 |
| 5 | 공정 순서 규칙 (1.5) | L | 하 | 교육 가치 최상 |
| 6 | Metrology 보강 (1.6) | L/S | 하 | |
| 7 | Write cap (2.1) | E | 하 | 코드 몇 줄, 효과 큼 |
| 8 | Read/charge sharing (2.2) | E | 상 | 상태기계 |
| 9 | Refresh 논리 (2.3) | L | 하 | 2.2에 의존 |
| 10 | 지수 누설+온도 (2.4) | S | 하 | |

# 5. 근거 수준에 대한 주석

본 문서의 물리는 Deal & Grove(1965)의 선형–포물선 모델과 표준 공정/소자 교과서
(예: Jaeger, Campbell, Sze) 수준의 내용이다. 수치 파라미터는 1000°C 대표값이며,
선택비·conformality·온도 한계 테이블은 공정·장비·조건에 따라 크게 달라지는
일반화 값이므로 **전부 소유자 리뷰 대상**이다. 이 프로젝트의 목표는 정량 재현이
아니라 인과관계의 방향과 자릿수(order of magnitude)를 올바르게 전달하는 것이며,
모든 구현은 그 기준으로 판단한다.
